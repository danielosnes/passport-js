/*

*/
/****************************************************************************************************************************************************************/

/*
By this point in the lesson, you might have noticed that there are a few values missing in the [movies] table.
More often than not, the data you encounter will have missing values.

Unknown values are indicated by [NULL]

It is not possible to test for [NULL] values with ocmparison operators such as [=] and [!=],

Instead, we use these operators:

[IS NULL]
[IS NOT NULL]

To filter for all movies WITH an imdb_rating:

SELECT name FROM movies WHERE imdb_rating IS NOT NULL;


*/
/****************************************************************************************************************************************************************/

/*
Instructions
Checkpoint 1 Passed
1.
Now let’s do the opposite.

Write a query to find all the movies without an IMDb rating.

Select only the name column!

We want to query for movies that have a missing value in their imdb_rating field:

SELECT name
FROM movies
WHERE imdb_rating IS NULL;

Copy to Clipboard

Notice how we used IS NULL instead of IS NOT NULL here.
*/
/****************************************************************************************************************************************************************/

/*
SELECT name FROM movies WHERE imdb_rating IS NULL;

name
The Good, the Bad and the Ugly
Dawn of the Dead
Shawn of the Dead
Star Wars: The Last Jedi
Ali Wong: Baby Cobra
The Big Sick
Deadpool
*/
/****************************************************************************************************************************************************************/
